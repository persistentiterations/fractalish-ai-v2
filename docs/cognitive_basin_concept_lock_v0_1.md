# Cognitive Basin Concept Lock v0.1

Locked doctrine for spec-first builds. Not consciousness. Not a chatbot. Operator sovereignty always.

## 1. FractalMemoryMap status

FractalMemoryMap is **not optional doctrine**. It is a canonical architectural layer that still needs full implementation.

- **CIRCUIT** stores memory nodes, contradiction scars, recovery routes, open loops, unresolved HOLDs.
- **FractalMemoryMap** is the **retrieval topology over CIRCUIT** — the basin's retrieval fabric.

It organizes CIRCUIT records into navigable multi-scale topology using:

- local semantic neighborhoods
- salience-weighted hubs
- replay-validated routes
- contradiction links
- HOLD/fog regions
- long-range shortcuts
- active attractor distance
- uncertainty gradients

FractalMemoryMap is **not**:

- vector nearest-neighbor search
- flat chat memory
- generic graph memory
- decorative visualization
- proof of consciousness

v0.1 micro-build: contract + minimal transparent ranking only. No full visualization.

## 2. InfinitySight / MNMVE status

InfinitySight / MNMVE is the **machine-native receptor/perception embodiment**. Not merely a vision mock.

Role: convert machine-native sensor streams into structured tokens that enter PERCEPT.

Upstream of Cognitive Basin. Must not bypass PERCEPT, RIGOR, GUARD, operator constraints, or SessionGlyph.

```
InfinitySight / MNMVE / Receptor Layer
  → ReceptorEvent
  → BasinLink
  → PERCEPT
  → ATAL
  → RIGOR
  → CIRCUIT
  → GUARD
  → SERA
  → SessionGlyph
```

## 3. Vendor-neutral device naming (canon)

| Name | Role |
|------|------|
| **Fractalish Field System** | Whole participant/device ecosystem |
| **Fractalish Field App** | Phone/local app: capture inputs, show basin state, emit ReceptorEvents |
| **Fractalish ReceptorCase** | Enhanced case: sensors, battery, external signal capture |
| **Fractalish FieldNode** | Any node: phone, phone+case, desktop simulator, sensor box |
| **ReceptorPack** | Modular sensor/power package |
| **ReceptorEvent** | Normalized local intake event |
| **BasinLink** | Bridge from ReceptorEvent into PERCEPT and SessionGlyph continuity |
| **InfinitySight / MNMVE** | Machine-native perception/receptor logic |

Never call these: Grok devices, xAI devices, OpenAI devices, Apple devices, Google devices, proprietary assistant devices, consciousness devices, AGI sensors.

## 4. Correct next step

Do **not** build the full product yet.

Do perform **spec-first micro-build**:

1. FractalMemoryMap schema/contract
2. ReceptorEvent → PERCEPT schema/contract
3. Layer-collapse tests

No app. No hardware. No visualization beyond optional JSON inspection.

Natural Math v3.6 remains trunk. v3.8 remains experimental branch. HOLD is sacred.